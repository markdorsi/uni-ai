{
  "buildCommand": "cd ../.. && npm run build && cd examples/nextjs-chat && npm run build",
  "installCommand": "cd ../.. && npm install && cd examples/nextjs-chat && npm install",
  "framework": "nextjs",
  "env": {
    "OPENAI_API_KEY": "@openai-api-key"
  }
}
